{
  "name": "json-as-xlsx-styled",
  "private": false,
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel --stream start",
    "build": "lerna run --stream build",
    "test": "lerna run --stream --no-bail test",
    "static": "cp -R packages/demo-reactjs/build/ build/ && cp CNAME build/CNAME"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^6.1.0"
  }
}
